import { makeStyles } from "@mui/styles";

export const ShopCategoryItemStyle=makeStyles({
    categorylist:{
        padding:2,
        textTransform: 'capitalize', // Capitalize the first letter of each word
    },

  
})