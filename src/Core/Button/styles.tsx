import { makeStyles } from "@mui/styles";

export const styles = makeStyles({
    container: {
        marginTop: "10px",
        marginBottom: "10px",
    },
    button: {
        backgroundColor: 'green'
    }
})